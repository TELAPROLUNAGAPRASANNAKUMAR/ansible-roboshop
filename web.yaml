- name: install web component
  hosts: web
  tasks:
- name: install nginx
  ansible.builtin.yum:
    name: nginx
    state: installed

- name: start and enable service
  ansible.builtin.service:
    name: nginx
    state: started
    enabled: true